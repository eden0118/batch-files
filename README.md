# 批次檔案重新命名工具 (Batch Renamer)

> 支援繁簡轉換、文本替換、符號移除、前綴後綴等功能的跨平台檔案批次重新命名工具

![Version](https://img.shields.io/badge/version-1.0-blue)
![Python](https://img.shields.io/badge/python-3.10+-brightgreen)
![License](https://img.shields.io/badge/license-MIT-green)

## 功能特性

✨ **核心功能**
- 🌐 **簡體轉繁體**: 自動轉換檔案名稱中的簡體字為繁體字
- 🔄 **文本替換**: 批次替換檔案名稱中的指定文本
- ✂️ **符號移除**: 移除檔案名稱中的特定符號字元
- 📝 **前綴後綴**: 為檔案名稱添加前綴和後綴
- 👁️ **實時預覽**: 執行前查看所有重新命名結果
- 🔍 **智能篩選**: 按副檔名或檔案類型進行篩選
- 📊 **詳細日誌**: 記錄每個檔案的重新命名過程

## 系統需求

### 開發環境
- **Python**: 3.10 或更高版本
- **作業系統**: macOS 10.14+, Windows 10+, Linux
- **依賴**: Flet (跨平台 GUI 框架)

### 執行環境
- **macOS**: 10.14 或更高版本 (Apple Silicon 和 Intel 支援)
- **Windows**: Windows 10 或更高版本

## 安裝

### 方法 1: 下載預編譯版本 (推薦)

前往 [Releases](https://github.com/MestoChang/batch-files/releases) 頁面下載對應系統的應用程式:
- **macOS**: `Batch Renamer.dmg` (App Bundle)
- **Windows**: `Batch Renamer.exe`

### 方法 2: 從原始碼執行

```bash
# 克隆儲存庫
git clone https://github.com/MestoChang/batch-files.git
cd batch-files

# 安裝依賴
pip install -r requirements.txt

# 執行應用程式
python3 flet/main.py
```

### 方法 3: 自行編譯

```bash
# 安裝編譯依賴
pip install -r requirements-build.txt

# macOS
bash build_mac.sh

# Windows (Command Prompt)
build_win.bat
```

## 使用指南

### 基本步驟

1. **選擇資料夾**: 點選「選擇資料夾」按鈕選擇要重新命名的目錄
2. **設定篩選條件** (可選):
   - **篩選類型**: 選擇「全部」、「僅檔案」或「僅資料夾」
   - **副檔名篩選** (可選): 輸入副檔名 (如 `txt,pdf`)
3. **設定重新命名操作**:
   - **轉換模式**: 無、簡轉繁、文本替換
   - **操作選項**: 移除符號、添加前綴/後綴
4. **預覽結果**: 檢查預覽視窗確認所有變更
5. **執行重新命名**: 點選「執行重新命名」按鈕

### 轉換模式詳解

#### 無轉換
僅應用格式化操作 (前綴、後綴、符號移除)

#### 簡轉繁
將檔案名稱中的簡體字轉換為繁體字
- 使用 OpenCC 函式庫進行高精度轉換
- 無 OpenCC 時自動使用備用字典

**範例**:
```
国家电力.txt → 國家電力.txt
书架上的爱好.pdf → 書架上的愛好.pdf
```

#### 文本替換
直接替換檔案名稱中的指定文本

**範例**:
```
尋找: "photo"
替換為: "image"
photo_2024.jpg → image_2024.jpg
```

### 格式化操作

#### 移除符號
輸入要移除的符號字元，以逗號或空格分隔

**範例**:
```
移除符號: "-_"
photo-2024_01.jpg → photo202401.jpg
```

#### 前綴和後綴
- **前綴**: 添加至檔案名稱開頭
- **後綴**: 添加至副檔名之前

**範例**:
```
前綴: "backup_"
後綴: "_v2"
document.docx → backup_document_v2.docx
```

### 篩選選項

#### 副檔名篩選
多個副檔名以逗號分隔，不需輸入點 (.)

**範例**:
```
篩選: "txt,pdf,docx"
只處理 .txt, .pdf, .docx 檔案
```

#### 檔案類型篩選
- **全部**: 處理檔案和資料夾
- **僅檔案**: 只處理檔案
- **僅資料夾**: 只處理資料夾

## 專案結構

```
batch-files/
├── flet/
│   └── main.py              # 主應用程式
├── thinter/
│   ├── batch_rename_gui.py  # Tkinter 版本 (歷史)
│   ├── i18n.py              # 國際化模組
│   └── styles.py            # 樣式定義
├── build_mac.sh             # macOS 編譯腳本
├── build_win.bat            # Windows 編譯腳本
├── build_spec.py            # PyInstaller 配置
├── requirements.txt         # 執行時依賴
├── requirements-build.txt   # 編譯依賴
└── README.md                # 本文件
```

## 依賴管理

### 執行時依賴 (`requirements.txt`)
```
flet>=0.28.0                 # GUI 框架
opencc-python-reimplemented  # 簡繁轉換 (可選)
```

### 編譯依賴 (`requirements-build.txt`)
```
flet>=0.28.0
pyinstaller>=6.0.0
```

## 編譯說明

### macOS 編譯

```bash
# 使應用腳本可執行
chmod +x build_mac.sh

# 執行編譯
./build_mac.sh
```

**輸出**:
- `dist/Batch Renamer.app` - 應用程式套件
- `dist/Batch Renamer.dmg` - DMG 安裝映像 (可選)

### Windows 編譯

```cmd
# 使用 Command Prompt 執行
build_win.bat
```

**輸出**:
- `dist/Batch Renamer.exe` - 可執行檔案

### 編譯配置

編輯 `build_spec.py` 自訂:
- 應用程式圖標 (icon 參數)
- 代碼簽署 (codesign_identity)
- 額外資源 (datas 參數)

## 常見問題 (FAQ)

### Q: macOS 上執行 .app 時出現「無法開啟應用程式」錯誤

**A**: 這是因為應用程式未簽署。使用以下指令:
```bash
# 移除隔離屬性
xattr -d com.apple.quarantine "/Applications/Batch Renamer.app"

# 或更改權限
chmod +x "/Applications/Batch Renamer.app/Contents/MacOS/Batch Renamer"
```

### Q: 簡轉繁轉換不夠準確

**A**:
1. 安裝 OpenCC 以獲得更好的轉換質量:
   ```bash
   pip install opencc
   ```
2. 應用程式會自動偵測並使用 OpenCC

### Q: Windows 編譯失敗

**A**: 檢查:
1. Python 已新增至 PATH
2. 以管理員身份執行命令提示符
3. 防毒軟體未攔截 PyInstaller

### Q: 重新命名操作無法復原

**A**: 執行重新命名前**務必**檢查預覽。使用檔案系統的版本控制或備份:
```bash
# 建議備份重要檔案
cp -r /path/to/folder /path/to/folder.backup
```

## 開發

### 環境設定

```bash
# 建立虛擬環境
python3 -m venv venv
source venv/bin/activate  # macOS/Linux
# 或
venv\Scripts\activate     # Windows

# 安裝依賴
pip install -r requirements.txt -r requirements-build.txt
```

### 執行開發版本

```bash
python3 flet/main.py
```

### 代碼風格

本專案遵循:
- **命名**: snake_case (函數/變數), CamelCase (類別)
- **類型提示**: 使用 `typing` 模組
- **文檔**: Google 風格的 docstring

### 貢獻指南

1. Fork 本倉庫
2. 建立特性分支 (`git checkout -b feature/AmazingFeature`)
3. Commit 變更 (`git commit -m 'Add AmazingFeature'`)
4. Push 至分支 (`git push origin feature/AmazingFeature`)
5. 開啟 Pull Request

## 變更日誌

### v1.0 (2024-01-18)
- ✨ 首個發行版本
- 🎨 Flet 跨平台 GUI
- 🌐 簡轉繁、文本替換等核心功能
- 📦 macOS 和 Windows 編譯支援
- 📝 詳細的預覽和日誌功能

## 許可證

MIT License - 詳見 [LICENSE](LICENSE) 文件

## 聯繫方式

- **問題回報**: [GitHub Issues](https://github.com/MestoChang/batch-files/issues)
- **討論**: [GitHub Discussions](https://github.com/MestoChang/batch-files/discussions)
- **Email**: (如有需要)

## 致謝

- [Flet](https://flet.dev/) - 跨平台 GUI 框架
- [OpenCC](https://github.com/BYVoid/OpenCC) - 簡繁轉換引擎
- 社群貢獻者

---

**Made with ❤️ by the Batch Renamer Team**

Last Updated: 2024-01-18
